# We use modules to better organize code
# We break the code into separate files, where each file is called module 

import Converters
from Converters import kg_to_lbs

kg_to_lbs(100)
print(Converters.kg_to_lbs(70))